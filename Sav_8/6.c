#include <stdio.h>

#define SIZE 100

void *myMalloc(size_t size);

void *blocksStart[SIZE];
size_t blocksSize[SIZE];
int count = 0;

int main() {
    

    return 0;
}

void *myMalloc(size_t size) {
    blocksStart
}